// Command tgproxy-panel runs the admin web panel and Telegram bot that issue
// and revoke tproxy-server proxy profiles.
//
// Wiring lands in later commits (see CLAUDE.md for the build sequence); this
// placeholder only exists so `go build ./...` succeeds from the first commit.
package main

import "log"

func main() {
	log.Println("tgproxy-panel: not yet implemented")
}
